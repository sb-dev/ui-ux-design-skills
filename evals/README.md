# Evals

This directory will contain focused command, skill, end-to-end and Extension Pack evaluations.

Initial rule: every P0 command must eventually have at least one focused normal case and one meaningful boundary/forbidden-behaviour case. Self-evaluation is not benchmark evidence.

The exact quality contract is `docs/04-testing-and-benchmark-spec.md`.
