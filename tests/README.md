# Tests

At scaffold stage, `npm test` runs zero-dependency repository and skill-contract validation.

Add deterministic unit tests here only when native scripts or artifact validators are implemented. Semantic UX evaluation belongs in `evals/` and `benchmarks/`, not in fake deterministic tests.
